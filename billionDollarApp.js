const billionDollarApp = (ideas) => {
  let futureCurrency = ideas;

  // input 100 ideas
  // 5 are sticky
  // 1 generates a billion dollar

  return 1000000000;
};

const result = billionDollarApp("money");
console.log(result);
